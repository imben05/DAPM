@model IEnumerable<FlightManagement.Models.Ticket>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        @TempData["SuccessMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
<div class="card-text">
    <a class="btn btn-success" href="/Ticket/Create" style="padding: 5px">Thêm mới</a>
</div>
<table class="table">
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ticketNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.seatNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Flight.flightNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Passenger.firstName)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.ticketNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.seatNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.price)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.status)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Flight.flightNumber)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Passenger.firstName)
                </td>
                <td>
                    @Html.ActionLink("Chỉnh sửa", "Edit", new { id = item.ticketID }, new { @class = "btn btn-primary" }) |
                    @Html.ActionLink("Xóa", "Delete", new { id = item.ticketID }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }

    </table>
